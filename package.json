{
  "name": "agentsync-remote",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker MCP server that makes claude.ai mobile a peer in the agentsync coordination mesh.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.20.0"
  }
}
